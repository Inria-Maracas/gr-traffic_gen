This is the traffic_gen-write-a-block package meant as a guide to building
out-of-tree packages. To use the traffic_gen blocks, the Python namespaces
is in 'traffic_gen', which is imported as:

    import traffic_gen

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(traffic_gen)
